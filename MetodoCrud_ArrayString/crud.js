/*

let lista = []; // Inicializamos una lista vacía

// CREATE AGREGAR
function agregarElemento(elemento) {
    lista.push(elemento); // Agregamos el elemento al final de la lista
}
 



let gaseosas = ["cocacola", "pepsi", "paso de los toros"]
*/

/* 
function agregarGaseosa(nuevaGaseosa){
    gaseosas.push(nuevaGaseosa)
} 

agregarGaseosa("fanta");
agregarGaseosa("sprite"); 

*/
 // READ 

 
/*
function mostrarGaseosas(){
for(i=0;i<gaseosas.length;i++){
    console.log(gaseosas[i])
} 
}  


// READ LECTURA
function mostrarElementos()
{
    for(i=0;i<lista.length;i++)
    {
        console.log(lista[i])   
    }
    
}

*/ 


/*

// UPDATE -> actualizar
function actualizarElemento(indice, nuevoElemento) {
    if (indice >= 0 && indice < lista.length) {
        lista[indice] = nuevoElemento; // Actualizamos el elemento en la posición especificada
    } else {
        console.log("Índice fuera de rango.");
    }
}

*/ 

/*

function actualizarGaseosa(ingreso,nuevaGaseosaIngresada){
if(indice>=0 && indice<gaseosas.length){
gaseosas[ingreso] = nuevaGaseosaIngresada
} else{
    console.log("valor fuera de rango")
}
}
mostrarGaseosas();
actualizarGaseosa(2,"rrrrrrr")
mostrarGaseosas()




/*


// DELETE -> BORRAR

function eliminarElemento(indice) {
    if (indice >= 0 && indice < lista.length) {
        lista.splice(indice, 1); // Eliminamos un elemento en la posición especificada
    } else {
        console.log("Índice fuera de rango.");
    }
}

// Ejemplo de uso
agregarElemento("elemento1");
agregarElemento("elemento2");
mostrarElementos();
console.log("Actualizando...")
actualizarElemento(1, "nuevoElemento");
mostrarElementos(); // Mostrará ["elemento1", "nuevoElemento"]
console.log("Eliminando...")
eliminarElemento(0)
mostrarElementos();


*/ 

/*
function eliminarGaseosa(indice){
if(indice>=0 && indice<gaseosas.length){
    gaseosas.splice(indice,1) } else{
        console.log("indice fuera de rango")
    }
} 

eliminarGaseosa(2)
mostrarGaseosas()
eliminarGaseosa(0)
mostrarGaseosas()


*/ 

/* 
// implementar la funcion de mostrar y mostrar con for (no con foreach)
// implementar la funcion de agregar, y agregar 3 frutas
// implementar la funcion eliminar y eliminar 1 fruta
// implementar la funcion actualizar y actualizar 2 frutas 
*/





